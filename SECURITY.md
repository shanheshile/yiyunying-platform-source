# 安全策略

## 报告安全问题

请通过项目所有者提供的私有渠道报告漏洞，不要在公开 Issue 中粘贴账号、Token、服务器地址、数据库快照或用户聊天内容。

## 源码安全基线

- 不提交 `.env`、签名文件、私钥、生产数据库、线上安装包或服务器备份。
- 生产配置必须通过环境变量、密钥管理服务或 CI Secret 注入。
- `backend/database/install.sql` 仅用于本地开发；上线前删除演示账号并轮换所有凭据。
- Android release 包必须使用独立签名，签名材料不得进入仓库。
- 上传文件必须校验大小、MIME、扩展名、内容摘要和实际文件头；可执行包及源码投稿应接入恶意软件扫描。
- 1、2、3 角色的审计能力必须记录操作者、目标、时间、IP、设备和原因，不能只依赖前端显示。

## 发布前检查

运行：

```powershell
.\scripts\secret-scan.ps1
.\scripts\verify.ps1
```

若发现疑似密钥，不得通过简单改名绕过检查，应先确认来源并轮换已泄露的凭据。
