# YIYUNYING_INTERNAL_APKS_AUTH_REQUEST_V1
# Rendered by deploy-internal-apks.py. This fragment belongs inside the HTTPS
# server block and deliberately contains no secret value.

include __YY_SECRET_INCLUDE__;

location = /__internal-apks-auth {
    internal;
    access_log off;
    fastcgi_pass __YY_FPM_UPSTREAM__;
    fastcgi_pass_request_body off;
    fastcgi_param CONTENT_LENGTH "";
    fastcgi_param SCRIPT_FILENAME __YY_PRIVATE_ROOT__/current/_auth/verify.php;
    fastcgi_param SCRIPT_NAME /__internal-apks-auth;
    fastcgi_param REQUEST_METHOD GET;
    fastcgi_param QUERY_STRING "";
    fastcgi_param YY_INTERNAL_VERIFIER 1;
    fastcgi_param YY_INTERNAL_ORIGINAL_METHOD $request_method;
    fastcgi_param YY_INTERNAL_ORIGINAL_REQUEST_URI $request_uri;
    fastcgi_param YY_INTERNAL_DOWNLOAD_SECRET $yy_internal_download_secret;
}

location @yy_internal_apk_not_found {
    access_log off;
    add_header Cache-Control "private, no-store, no-cache, must-revalidate, max-age=0" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Robots-Tag "noindex, noarchive" always;
    return 404;
}

location @yy_internal_apk_expired {
    access_log off;
    add_header Cache-Control "private, no-store, no-cache, must-revalidate, max-age=0" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Robots-Tag "noindex, noarchive" always;
    return 410;
}

location ~ ^/__internal-apks/debug/2\.7\.15/(?<apk>yiyunying-(?:user|admin|authorized-platform|platform-owner)-v2\.7\.15-debug\.apk)$ {
    auth_request /__internal-apks-auth;
    error_page 401 =404 @yy_internal_apk_not_found;
    error_page 403 =410 @yy_internal_apk_expired;

    alias __YY_PRIVATE_ROOT__/current/debug/2.7.15/$apk;
    disable_symlinks on;
    limit_except GET { deny all; }
    types { application/vnd.android.package-archive apk; }
    default_type application/vnd.android.package-archive;
    gzip off;
    etag on;
    if_modified_since exact;
    max_ranges 1;
    access_log off;
    log_not_found off;
    add_header Content-Disposition "attachment" always;
    add_header Cache-Control "private, no-store, no-cache, must-revalidate, no-transform, max-age=0" always;
    add_header Pragma "no-cache" always;
    add_header Expires "0" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer" always;
    add_header X-Robots-Tag "noindex, noarchive" always;
}

location ~ ^/__internal-apks/candidate/1\.0\.0/(?<apk>yiyunying-(?:user|admin|authorized-platform|platform-owner)-v1\.0\.0\.apk)$ {
    auth_request /__internal-apks-auth;
    error_page 401 =404 @yy_internal_apk_not_found;
    error_page 403 =410 @yy_internal_apk_expired;

    alias __YY_PRIVATE_ROOT__/current/candidate/1.0.0/$apk;
    disable_symlinks on;
    limit_except GET { deny all; }
    types { application/vnd.android.package-archive apk; }
    default_type application/vnd.android.package-archive;
    gzip off;
    etag on;
    if_modified_since exact;
    max_ranges 1;
    access_log off;
    log_not_found off;
    add_header Content-Disposition "attachment" always;
    add_header Cache-Control "private, no-store, no-cache, must-revalidate, no-transform, max-age=0" always;
    add_header Pragma "no-cache" always;
    add_header Expires "0" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer" always;
    add_header X-Robots-Tag "noindex, noarchive" always;
}

# Do not change this fallback to ^~: the two strict regex locations above must
# be considered first. All other versions, roles, suffixes and paths fail shut.
location = /__internal-apks {
    access_log off;
    return 404;
}

location /__internal-apks/ {
    access_log off;
    log_not_found off;
    return 404;
}
