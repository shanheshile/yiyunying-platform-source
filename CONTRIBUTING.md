# 参与开发

1. 从 `main` 创建功能分支。
2. 一个提交只处理一个可回滚的主题，避免混入构建产物和生产数据。
3. UI 修改必须验证浅色、深色、跟随系统、刘海屏、手势导航和三键导航。
4. 聊天、通知、资金、权限和通话修改必须补充状态边界测试。
5. 提交前运行 `scripts/verify.ps1`，并在说明中列出无法自动验证的真机项目。
6. 用户昵称、备注和账号名称属于数据，不参与界面语言翻译。

代码评审优先检查：权限绕过、状态重复提交、Activity/Fragment 生命周期、滚动位置丢失、媒体内存占用、底部安全区和本地化缺失。
