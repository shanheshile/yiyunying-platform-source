# 易云盈平台全量需求与实施总纲

更新基线：`2.7.5 (50)`
建立日期：2026-08-01
适用范围：Android 用户端（4）、三级管理端（1/2/3）、PHP API、MySQL、下载站、更新链路与运维平台

## 1. 文档用途与状态规则

本文件把历次需求合并为唯一的产品与工程检查表。它不是“所有需求已经完成”的声明；任何能力只有同时具备客户端、服务端、数据库、权限、异常处理、测试和真实环境验收，才允许标为完成。

状态只使用以下四种：

| 状态 | 含义 |
| --- | --- |
| 已完成 | 已形成端到端闭环，并留有可重复的测试或发布证据 |
| 部分完成 | 已有代码或页面，但仍缺服务端、边界、性能、权限或真机验收 |
| 待实现 | 需求已确认，尚无足够实现证据 |
| 阻塞 | 依赖外部服务、生产凭据、合规确认或用户真机验收 |

每项需求必须具备：需求编号、角色范围、当前状态、实现措施、验收标准、风险和证据链接。页面、按钮、接口占位或静态文案均不能单独算完成。

## 2. 产品角色、租户与继承模型

| 编号 | 目标 | 当前状态 | 实施与验收 |
| --- | --- | --- | --- |
| GOV-001 | 1 为最高平台方，2/3 管理自己的下级与应用，4 为普通用户 | 部分完成 | 统一 RBAC/ABAC；接口按 `tenant_id/app_id/role` 复核；越权矩阵全部通过 |
| GOV-002 | 管理端最左侧只展示当前账号拥有或获授权的应用 | 待实现 | 应用列表来自权限服务，禁止只做前端过滤；跨租户请求返回统一错误码 |
| GOV-003 | 2/3 可继承上级模块，也可维护自己的模块和更新策略 | 待实现 | 模块建立来源、版本、继承、覆盖和回滚关系；下级只能看获授权版本 |
| GOV-004 | 每个应用内按用户、论坛、动态、文件、资金等业务系统管理 | 部分完成 | 建立领域导航与类型化详情页，不展示原始 JSON；批量动作带确认和审计 |
| GOV-005 | 管理员代入用户视角查看或系统发言 | 部分完成 | 必须显示代入状态和系统身份；记录理由、操作者、前后值、设备及关联对象 |
| GOV-006 | 管理端文档系统可发布接口文档和开发模块 | 待实现 | 文档结构化存储、版本控制、权限、示例、废弃标识和脱敏，禁止展示真实密钥 |
| GOV-007 | 所有用户端功能都可由上级配置开关、默认值和上限 | 部分完成 | 建立功能策略中心；客户端仅展示，服务端强制执行；策略变更可审计、灰度和回滚 |

## 3. 消息、会话、搜索与离线浏览

| 编号 | 需求 | 当前状态 | 关键措施与验收 |
| --- | --- | --- | --- |
| MSG-001 | 私聊、群聊、聊天室、客服及时收发且弱网不重复 | 部分完成 | 统一消息状态机、服务端递增游标、幂等键、WebSocket 主链和 HTTP 增量降级；确认 P95 <= 800ms |
| MSG-002 | 空白会话进入即加载，不需先发一条消息 | 部分完成 | 本地快照先渲染，再按游标增量补齐；冷启动与断网回归测试 |
| MSG-003 | 上翻历史时不自动跳底；新消息显示“新消息（N）” | 部分完成 | 稳定 ID、差量更新、滚动锚点；只有用户点击锚点才回到底部 |
| MSG-004 | 打开键盘、Emoji、更多功能不改变当前阅读位置 | 部分完成 | 输入区高度变化只调整 inset，不执行强制滚动；覆盖横竖屏和大字体 |
| MSG-005 | 草稿置顶并显示橙色 `[草稿]内容`，删除后恢复原排序 | 部分完成 | 草稿按账号和会话隔离持久化；列表排序与清除状态测试 |
| MSG-006 | 消息列表预览区分红包、文件、语音、媒体等类型 | 部分完成 | 统一消息摘要生成器，颜色仅作辅助且保证深浅主题对比度 |
| MSG-007 | 聊天记录按五分钟、跨日/周/月/年显示时间节点 | 已完成 | `2.7.5` 已加入格式器、布局和单元测试；仍需真机视觉验收 |
| MSG-008 | 搜索 UID、账号名、备注、群聊与内容类型 | 部分完成 | 本地 FTS + 服务端搜索；权限过滤后再返回；离线只搜缓存范围 |
| MSG-009 | 转发区间使用上下两条横向虚线和起止锚点 | 部分完成 | 锚点随首尾消息显示，嵌套快照只读；匿名规则逐层独立生效 |
| MSG-010 | 离线可查看缓存会话、资料和附件但不能误发 | 部分完成 | 账号隔离数据库、只读离线状态、发送队列显式确认；恢复网络后增量同步 |
| MSG-011 | 长按菜单按消息类型提供不同动作 | 部分完成 | 文本、语音、表情、文件、系统消息分别定义动作；小尺寸毛玻璃菜单且不截字 |
| MSG-012 | 系统提示可删除，撤回等记录保持简洁 | 部分完成 | 删除仅影响获授权范围；不可删除审计节点与时间节点 |

## 4. 媒体、文件、语音、收藏与剪贴板

| 编号 | 需求 | 当前状态 | 关键措施与验收 |
| --- | --- | --- | --- |
| MED-001 | 图片、GIF、动态照片、视频、语音、音频、文档和文件语义独立 | 部分完成 | 统一媒体模型和 MIME 校验；GIF 与动态照片不能互相冒充 |
| MED-002 | 相册分页、预览、左右滑动、多选和返回状态保持 | 部分完成 | 自建选择器分页渲染；预览和网格共用选择仓库；只有关闭整个选择流程才清空 |
| MED-003 | 滑动多选支持边缘自动滚动，中间停下仅浏览 | 部分完成 | 手势状态机区分选择/浏览；方向、速度、触控冲突和无障碍测试 |
| MED-004 | 原图显示单项或总大小，预览显示当前媒体大小 | 部分完成 | 上传前读取元数据；视频与图片共用大小汇总规则 |
| MED-005 | 多图堆叠显示层次、张数、当前序号并可展开收起 | 部分完成 | 固定容器和层间距；横竖图视觉面积接近；索引边界回归测试 |
| MED-006 | 图片预览支持原图、GIF 动画、左右切换和自然过渡 | 部分完成 | 缩略图先显、原图渐进加载；动画图保留原始帧；关闭/原图使用图标按钮 |
| MED-007 | 视频有封面、横竖屏、全屏、锁定、手势进度和沉浸式播放 | 部分完成 | Media3 单实例播放器；手势方向与进度一致；切页不误中断其他音频 |
| MED-008 | 视频自动播放和缓存可按 Wi-Fi/流量/从不配置 | 部分完成 | 本地用户策略与管理端上限合并；流量变化即时生效 |
| MED-009 | 语音显示波形、时长、滑动取消范围和 1/1.5/2/3 倍速 | 部分完成 | 波形数据持久化；倍速按钮主题对比度和文字完整性真机验收 |
| MED-010 | 已发语音转写自动渲染但可收起；编辑框支持中英混合识别 | 部分完成 | 离线优先、在线增强；无网才提示降级；3 秒静音自动结束 |
| MED-011 | 音频文件与语音消息使用不同播放器 | 部分完成 | 音频使用标准时间轴、倍速和后台播放；语音使用波形卡片 |
| MED-012 | 自建文件选择器按来源、类型、时间和大小展示 | 部分完成 | 手机文件、聊天、收藏、相册、云端分区；分页、预览、多选和搜索 |
| MED-013 | 大文件分片、断点续传，避免 413 HTML 错误 | 部分完成 | 服务端发布上传限制；客户端分片和进度；错误统一 JSON 契约 |
| MED-014 | 收藏中心和聊天收藏内容相同但交互不同 | 部分完成 | 收藏中心短按详情/长按多选；聊天收藏短按发送/长按详情；共用数据源 |
| MED-015 | 收藏支持类型化详情与跳原位置两条路径 | 部分完成 | 图片、视频、聊天快照、帖子等专用渲染；禁止字段表和裸 JSON |
| MED-016 | 软件内复制媒体显示媒体本身，外部复制为受控文件 | 部分完成 | 使用 ClipData URI 和 FileProvider；不把服务器 URL 放入剪贴板 |
| MED-017 | 表情包独立预览、发送、添加、转发与删除 | 部分完成 | 表情只在同类中左右浏览，不参与图片堆叠；动画保持可播放 |

## 5. 通话、通知、后台与厂商适配

| 编号 | 需求 | 当前状态 | 关键措施与验收 |
| --- | --- | --- | --- |
| CALL-001 | 点击语音/视频后立即进入呼叫页 | 部分完成 | 本地预览先启动，信令异步建立；防重复点击与会话复用；建立 P95 <= 5s |
| CALL-002 | 双方都有声音与画面，默认焦段合理且画质自适应 | 阻塞 | 需 TURN/STUN 和真机联调；不能固定承诺所有设备 1080p/120fps |
| CALL-003 | 本地前置预览镜像，远端显示规则可配置且双方一致 | 部分完成 | 区分预览变换和发送帧变换；交换大小窗不改变镜像语义 |
| CALL-004 | 大小窗点击交换、可拖动小窗并平滑动画 | 部分完成 | 可中断动画、固定宽高比、安全边界与横竖屏恢复 |
| CALL-005 | 视频默认扬声器、语音默认听筒；调节通话音量 | 部分完成 | Android 音频焦点和通信模式；蓝牙/有线/听筒/扬声器路由测试 |
| CALL-006 | 通话时长从双方真正接通后开始且保持一致 | 部分完成 | 服务端 `connected_at` 为唯一时间源；悬浮窗和页面同一计时器 |
| CALL-007 | 任一方挂断、失败、超时均同步结束并三秒清理 | 部分完成 | 权威状态机、事件序号、幂等终止；清理页面、前台服务和唯一通知 |
| CALL-008 | 来电只保留一个高优先级通知，支持接听/挂断 | 部分完成 | 单一 channel + full-screen intent；通知动作幂等；不被普通提醒覆盖 |
| CALL-009 | 锁屏/后台来电、铃声、震动和厂商权限入口 | 阻塞 | 小米/vivo/OPPO/华为真机矩阵；按系统允许范围引导自启动和电池策略 |
| CALL-010 | 语音/视频支持画中画、悬浮窗、挂边和正确计时 | 部分完成 | PiP 与系统悬浮权限分开；视频比例、顶部安全区和自动关闭验收 |
| CALL-011 | 通话卡片作为发起方消息显示并原位更新结果 | 待实现 | 消息记录关联 call_id；结束后更新原卡片，不重复新增冲突系统消息 |
| CALL-012 | 多人语音/视频、语音通话可临时开启摄像头 | 待实现 | 采用 SFU 架构和成员状态模型；权限、带宽、主持人控制及容量测试 |
| CALL-013 | 视频镜像、美颜、摄像头关闭和沉浸式控制 | 部分完成 | GPU 滤镜可关闭；点击空白隐藏控件；关闭摄像头显示头像 |
| CALL-014 | 系统电话占线时只闭麦占线方并同步提示双方 | 部分完成 | Telecom/音频焦点监听；隐私友好的状态文案和恢复逻辑 |

## 6. 资料卡、好友、群组、二维码、位置与权限

| 编号 | 需求 | 当前状态 | 关键措施与验收 |
| --- | --- | --- | --- |
| PRO-001 | 聊天左上进入对应资料卡，右上进入权限管理 | 部分完成 | 按会话类型路由用户/群/聊天室；标题显示备注或账号名而非“私聊” |
| PRO-002 | 资料卡与权限页最多各一层，返回到真实来源 | 部分完成 | 单例导航键与 `CLEAR_TOP`；禁止资料/权限互相无限叠栈 |
| PRO-003 | 用户名、备注和用户内容永不自动翻译 | 部分完成 | 与界面资源文案分离；三语言回归测试 |
| PRO-004 | 好友列表、分组、特别关心和搜索 | 部分完成 | 默认“我的好友/特别关心”；自定义分组 CRUD；头像回退统一 |
| PRO-005 | 群聊默认“我创建的/我加入的”分组 | 部分完成 | 服务端来源字段、搜索、分组管理和离线快照 |
| PRO-006 | 邀请、转发、礼物等复用头像列表多选器 | 部分完成 | 已选头像横栏、数量、分组、搜索、单选/多选模式和状态恢复 |
| PRO-007 | 新成员历史记录权限和选择性分享记录 | 部分完成 | 群策略 + 邀请批次授权；服务端验证可见起点 |
| PRO-008 | 群主/管理员/成员、禁言、解禁、踢出和管理审计 | 部分完成 | 权限矩阵与原因；所有动作形成系统提示和不可篡改审计 |
| PRO-009 | 权限覆盖勿扰、特别关心、黑名单、添加方式和动态可见 | 部分完成 | 权限页类型化；服务端复核；无网只读不允许假保存 |
| PRO-010 | 仅指定/除指定外必须从好友分组和列表选择 | 部分完成 | 搜索只辅助，不能要求手输 ID；动态、帖子、笔记同步使用同一选择器 |
| PRO-011 | 我的好友码、扫码和相册识别合并 | 部分完成 | 竖屏、明确关闭/超时、个人/群/业务码分流，不无限加载 |
| PRO-012 | 添加好友入口同时支持新建群聊和聊天室 | 待实现 | 统一创建入口，标签可选择/申请；创建成功进入类型化资料页 |
| LOC-001 | 聊天、论坛、悬赏和动态可发送位置 | 部分完成 | 统一地点对象：坐标系、名称、地址、精度、时间和隐私范围 |
| LOC-002 | 软件内查看自己与对方位置、距离并拖动选点 | 阻塞 | 接入合规的国内地图 SDK；拒绝定位时允许搜索或手动选点 |
| LOC-003 | 可选择高德、百度或腾讯外部打开 | 部分完成 | 正确坐标转换与 URI 路由；无对应应用时给出明确选择 |

## 7. 动态、笔记、论坛、悬赏、投票、接龙与互动

| 编号 | 需求 | 当前状态 | 关键措施与验收 |
| --- | --- | --- | --- |
| CNT-001 | 生活动态九格图/视频、文字、位置、可见范围和详情 | 部分完成 | 媒体共九个槽；详情页沉浸显示当前动态和互动，不显示时间轴 |
| CNT-002 | 动态两分钟内仅编辑一次并显示编辑时间，可删除 | 部分完成 | 服务端时间和次数约束；删除记忆窗口与审计区分 |
| CNT-003 | 个人动态页分置顶和普通区，公共动态不显示置顶样式 | 部分完成 | 置顶只影响个人空间排序，不突破指定对象权限和屏蔽关系 |
| CNT-004 | 动态支持隐藏、删除、点赞头像、评论、回复、转发、收藏 | 部分完成 | 图标化动作；共同好友可见规则；头像进入资料卡 |
| CNT-005 | 笔记支持图文/附件、年月日查询和可见范围 | 部分完成 | 本地索引与服务端筛选；附件复用媒体模型 |
| CNT-006 | 论坛板块、二级分类、标签同义词和创建申请 | 部分完成 | 分类字典唯一化；申请、审核、版主与申诉闭环 |
| CNT-007 | 发帖内容节支持文字、图片、视频、音频、链接、文档和文件 | 部分完成 | 可排序内容块；保存草稿；发布前预览和权限检查 |
| CNT-008 | 评论可发 Emoji、图片、语音和附件 | 部分完成 | 评论编辑器复用聊天输入能力；附件选择精准进入对应类型 |
| CNT-009 | 主评论/副评论同框，默认预览两条并可展开 | 部分完成 | 层级上限、分页、折叠状态和无障碍语义；互动不中断音频播放 |
| CNT-010 | 帖子/评论可点赞、收藏、回复、转发、举报 | 部分完成 | 统一图标动作；转发评论附带帖子快照；个人与版主动作分离 |
| CNT-011 | 悬赏分类、富媒体、奖励类型和最长一月日期选择 | 部分完成 | 滚轮日期选择器；截止和奖励由服务端校验 |
| CNT-012 | 投票支持单选/多选、图片选项、有效期和结果策略 | 部分完成 | 活动发起按钮不得显示“发帖”；并发投票和重复提交测试 |
| CNT-013 | 群投票、活动投票、帖子/悬赏投票使用统一核心模型 | 待实现 | 场景适配层 + 统一投票服务，管理端按来源查看和审核 |
| CNT-014 | 接龙字段可扩展、序号由服务端原子分配 | 部分完成 | 慢提交自动获得下一个序号；每个接龙独立，不虚构有效期 |
| CNT-015 | 动态/帖子/悬赏/投票/红包等支持 @ 与定位跳转 | 部分完成 | 类型化 mention 对象；通知强度分级；点击精准定位原内容 |

## 8. 资源、商店、投稿、资金、红包、订单与奖励

| 编号 | 需求 | 当前状态 | 关键措施与验收 |
| --- | --- | --- | --- |
| COM-001 | 大类为资源、商店、余额商店；资源含应用商店/源码商城 | 部分完成 | 三个独立业务域和导航；禁止概念混用 |
| COM-002 | 应用投稿只选 APK/HAP/IPA/EXE 等安装包并提取元数据 | 部分完成 | 沙箱解析图标、名称、包名、版本、大小、权限；结果可人工校正 |
| COM-003 | 源码投稿识别语言、框架、平台和压缩包内容 | 部分完成 | MIME/扩展名/内容联合识别；递归扫描与依赖风险报告 |
| COM-004 | 分类可搜索/手输/自动推荐且同义分类唯一 | 部分完成 | 分类词典、同义词映射、审核和合并工具 |
| COM-005 | 投稿先隔离扫描，危险内容阻止公开并可追责 | 待实现 | 哈希、签名、杀毒、依赖、压缩炸弹和静态规则；记录用户/设备/IP/时间 |
| COM-006 | 商店/余额商店有分类、搜索、详情、库存、评论和购买 | 部分完成 | 余额商店只使用平台余额；订单和库存事务一致 |
| COM-007 | 所有详情使用类型化 UI，不显示 JSON | 部分完成 | 资源、红包、抽奖、订单、收藏、通知均注册专用渲染器 |
| FIN-001 | 红包支持按份、随机抢、指定范围、自己领取和运气王 | 部分完成 | 以分为整数单位；最低 0.01；金额守恒且不超领 |
| FIN-002 | 私发接收人可退回给发送者，发送者不能自退 | 部分完成 | 接收方/发送方和场景权限服务端判断；群发/活动按独立规则 |
| FIN-003 | 红包、转账、礼物 24 小时过期并退回剩余 | 部分完成 | 幂等定时任务、状态原位更新、可重放和对账 |
| FIN-004 | 红包领取完/退回/过期仍可打开详情且变灰 | 部分完成 | 状态型卡片与领取记录；详情显示份数、剩余、到期和运气王 |
| FIN-005 | 使用不可变双重记账账本 | 待实现 | 交易、冲正、退款、领取全用事务与幂等键；每日自动对账 |
| FIN-006 | 订单覆盖商品、余额商品、资源、付费帖子、会员和权益 | 部分完成 | 购买自动生成订单；状态、支付、交付、退款和售后全链追踪 |
| FIN-007 | 奖励规则支持注册、登录、签到、邀请、发帖和有效反馈 | 部分完成 | 可配置一次/每日次数、奖励类型、数量、上限和风控 |
| FIN-008 | 抽奖支持奖品、概率、数量、付费/免费次数和记录 | 部分完成 | 概率总和校验、库存事务、可验证随机、审计和详情 UI |

## 9. 全局设计、主题、语言、图标与设备布局

| 编号 | 需求 | 当前状态 | 关键措施与验收 |
| --- | --- | --- | --- |
| UI-001 | 深浅/跟随系统、主色、字体、语言、背景全局无感切换 | 部分完成 | 单一 AppearanceStore；安全重建；关键页面三语言/两主题截图回归 |
| UI-002 | 用户名、备注和用户内容不被界面语言翻译 | 部分完成 | 仅资源键进入翻译链；伪本地化检查 |
| UI-003 | 桌面图标只切换真实 alias，通知图标同步品牌风格 | 部分完成 | Manifest 自动核对；失败回滚；注意 Android 通知小图标仍受单色规范限制 |
| UI-004 | 默认/全局/单聊背景分层且可恢复默认 | 部分完成 | URI 持久权限、预加载、失败回退和主题对比遮罩 |
| UI-005 | 所有底部弹层主按钮保留完整四角圆角和安全区 | 部分完成 | 共享组件不覆盖 Material 背景后再调 Shape API；截图回归 |
| UI-006 | 消息长按与列表菜单小尺寸、毛玻璃、可读且触控完整 | 部分完成 | 字号、间距和 48dp 触控解耦；长文案省略/换行而不截断 |
| UI-007 | Dock 在小米不裁字，vivo 保持正常尺寸 | 已完成 | `2.7.5` 厂商策略单测通过；仍需对应真机截图验收 |
| UI-008 | 状态栏、刘海、手势区和键盘自适应 | 部分完成 | WindowInsets 单一处理；首页搜索不把底部 Dock 顶起 |
| UI-009 | 浅色/深色文字和控件始终有足够反差 | 部分完成 | 语义颜色 token + WCAG 对比检查；天气、倍速、弹层专项截图 |
| UI-010 | 页面切换、媒体交换和更多功能手势跟手 | 部分完成 | 速度预测、三分之一阈值、可中断吸附动画；性能预算和慢帧监控 |

## 10. 智能机器人、天气、新闻与语音识别

| 编号 | 需求 | 当前状态 | 关键措施与验收 |
| --- | --- | --- | --- |
| AI-001 | 任意城市天气，已给城市时不再要求定位 | 部分完成 | 行政区划库 + 地理编码消歧；兖州、济宁、曲阜分别解析 |
| AI-002 | 回答几点到几点下雨并列出逐小时区间 | 部分完成 | 使用逐小时降水数据；标注更新时间、来源和不确定性 |
| AI-003 | 历史、旅游、文学、科学、人文、地理、农学和哲学问答 | 部分完成 | 检索增强生成、来源排序、超时熔断和无来源拒答 |
| AI-004 | 新闻、今日快报和当地热点必须实时查询 | 部分完成 | 仅使用带时间戳来源；区分发布时间与事件发生时间 |
| AI-005 | 机器人编辑框被输入法正确顶起且发送按钮居中 | 部分完成 | edge-to-edge + ime inset；固定图标容器和触控范围 |
| AI-006 | 离线基础问答与在线复杂问答分层 | 待实现 | 小型本地模型处理隐私/基础意图；服务器模型处理工具和检索 |
| AI-007 | 天气卡片按天气状态动效且不卡顿 | 部分完成 | 轻量粒子/帧动画、降级静态背景和 60fps 性能门槛 |

## 11. 设置、通知中心、更新、维护与发布

| 编号 | 需求 | 当前状态 | 关键措施与验收 |
| --- | --- | --- | --- |
| SET-001 | 设置只有一个统一入口，功能按账号、安全、隐私、外观等分类 | 部分完成 | 去除首页重复项；站内搜索跳到唯一设置页 |
| SET-002 | 通知、来电与后台权限逐项直达真实系统位置 | 部分完成 | 各权限单独检测和 Intent；不支持直达时明确打开应用详情并说明路径 |
| SET-003 | 异地登录保护支持严苛/宽松、设备授权和一次性 PIN | 部分完成 | 风险引擎、设备列表、登录记录、远程注销和关闭状态持久化 |
| SET-004 | 通知中心按系统/动态/活动等类型展示专用详情 | 部分完成 | 红点实时归零；查看按钮进入关联业务，不只进入对方资料卡 |
| REL-001 | 手动检查和自动检查使用同一真实接口且无需重启 | 已完成 | `2.7.5` 已实现手动请求和统一比较；待公网接口验收 |
| REL-002 | 更新弹层不可误触关闭，通知栏显示下载进度 | 已完成 | `2.7.5` 已实现；Android 13 通知权限保护和四端 Lint 通过 |
| REL-003 | 更新显示版本、标题、内容、包名、大小、进度和频率 | 部分完成 | 管理端发布表单 + 客户端类型化 UI；频率由服务端与本地共同控制 |
| REL-004 | 维护可强制限制全局、部分功能或允许只读进入 | 部分完成 | 服务端维护策略必须优先于客户端缓存，且提供紧急恢复开关 |
| REL-005 | 源码、APK、更新接口、下载站和 GitHub 版本一致 | 部分完成 | 自动构建校验 Manifest/versionCode/SHA-256；公网复下载复算 |
| REL-006 | 每次发布自动自增新版本并记录历史 | 部分完成 | 发布脚本负责版本、更新说明、制品清单、标签和回滚点 |
| REL-007 | 生产包固定 release 签名，不公开 debug 包 | 阻塞 | 需创建/托管生产密钥并验证覆盖安装；当前 2.7.5 仍是验证签名包 |

## 12. 自动下载缓存与本地数据管理

| 编号 | 需求 | 当前状态 | 关键措施与验收 |
| --- | --- | --- | --- |
| CACHE-001 | 自动缓存默认开启，可按网络和媒体类型配置 | 部分完成 | Wi-Fi/流量/从不；图片/视频/音频/文件独立开关；管理端只设上限/默认值 |
| CACHE-002 | 可按好友、群、聊天室、类别和时间线清理 | 部分完成 | 预估释放空间、全选当前类、保留例外、二次确认和可取消任务 |
| CACHE-003 | 可清理本地聊天记录但不误删服务端记录 | 部分完成 | 明确“仅本机删除”；本地墓碑和重新下载策略；高风险操作需再次确认 |
| CACHE-004 | 缓存按账号隔离、加密并在退出/注销时按策略处理 | 部分完成 | 每账号密钥、容量配额、保留期限和退出提示 |
| CACHE-005 | 管理端配置允许类型、上限和默认策略 | 部分完成 | 服务端策略与用户选择合并，不能远程静默删除用户本地资料 |

## 13. 安全、数据库、可观测性与质量保障

| 编号 | 必须补充的能力 | 当前状态 | 实施要求 |
| --- | --- | --- | --- |
| SEC-001 | 轮换历史暴露的面板、SSH、数据库和 API 凭据 | 阻塞 | 改用密钥管理、最小权限、短期凭据和登录审计 |
| SEC-002 | 数据导出、删除、保留期限、隐私授权和远程注销 | 待实现 | 合法依据、身份复核、可追踪任务与结果回执 |
| SEC-003 | 管理员查看聊天和匿名身份须有合法理由与审计 | 部分完成 | 双人授权/高风险提醒、不可篡改日志和最小可见字段 |
| DB-001 | 所有表结构使用版本化迁移和可恢复备份 | 部分完成 | 发布前备份、迁移 dry-run、回滚或前向修复、RPO/RTO 演练 |
| API-001 | OpenAPI 契约，成功不返回 HTML，错误不泄漏堆栈 | 部分完成 | 统一错误码、request_id 和用户可读文案；生成客户端模型 |
| OBS-001 | 崩溃、ANR、慢帧、消息、通话、上传和资金监控 | 待实现 | 指标、告警、脱敏日志、采样和版本/设备维度看板 |
| PERF-001 | 启动、首页、列表、预览和弹层性能预算 | 部分完成 | 冷启动 P50 <=1.5s/P95 <=3s；慢帧 <5%；禁止主线程大 JSON/封面生成 |
| QA-001 | 小米/vivo/OPPO/华为/原生 Android 真机矩阵 | 部分完成 | Android 版本、屏幕、刘海、导航模式、字体大小、三语言和两主题 |
| QA-002 | 历史崩溃全部建立回归测试 | 部分完成 | Fragment 生命周期、LayoutParams、Glide 销毁期、索引、alias、MaterialButton 背景 |
| OPS-001 | 灰度、暂停、回滚、防降级和公网制品校验 | 部分完成 | 版本签名、防重放、下载哈希、发布清单和一键回滚演练 |
| OPS-002 | 完整项目备份与灾难恢复 | 部分完成 | 源码、迁移、配置模板、制品清单和文档入包；绝不把真实密码放入 JSON/ZIP |

## 14. 实施顺序

### P0：先保证不会丢数据、不会越权、不会扣错钱

1. `SEC-001`、`REL-007`：轮换凭据并固定生产签名。
2. `MSG-001~003`：消息游标、幂等与滚动锚点端到端闭环。
3. `CALL-001~009`：通话权威状态机、单一通知和三秒清理。
4. `FIN-001~006`：双重记账、事务幂等、24 小时退款和对账。
5. `GOV-001~007`：服务端权限统一，封堵跨租户与角色越权。
6. `API-001`、`DB-001`：契约、迁移、备份与恢复门禁。

### P1：完成用户高频主路径

1. 相册/文件/媒体/语音/收藏闭环和大文件续传。
2. 资料卡、权限、好友/群选择器和二维码/地图。
3. 动态、论坛、悬赏、投票和评论富媒体闭环。
4. 商店、资源投稿、订单、红包和抽奖类型化 UI。
5. 全局主题、语言、背景、图标、Dock 和底部弹层真机矩阵。

### P2：平台化与智能能力

1. 四级管理端模块继承、策略中心、文档系统和应用市场。
2. 智能机器人检索、天气/新闻工具、本地模型和评测集。
3. 多人通话、美颜、SFU、容量治理和质量监控。
4. 全量可观测性、自动化截图、灰度发布与灾难恢复演练。

## 15. 统一完成定义

任何编号只有同时满足以下条件才可改为“已完成”：

1. 需求、角色、权限和失败路径明确。
2. Android、API、数据库和管理端形成闭环。
3. 用户界面无裸 JSON、服务器地址、堆栈或英文内部枚举泄漏。
4. 有自动化测试覆盖正常、边界、弱网、并发和历史崩溃路径。
5. 小米和 vivo 至少各一台目标设备完成真机截图与交互验收；涉及后台/来电时扩展到 OPPO、华为和原生 Android。
6. 达到性能、安全、隐私、可访问性和数据一致性门槛。
7. 版本、安装包、签名、更新接口、下载站、GitHub、数据库迁移和回滚资料一致。
8. 发布后存在指标、告警、审计和可执行回滚方案。

## 16. 2.7.5 当前可验证基线

- 小米 Dock 使用单独字体度量和高度补偿，vivo 继续使用正常尺寸；策略单元测试已覆盖 Xiaomi/Redmi/POCO/vivo/OPPO。
- 聊天五分钟与跨日时间节点已进入消息布局并有格式化单元测试。
- 设置页“立即检查”使用真实更新接口；更新下载不可误触关闭并提供通知栏进度。
- 四角色 Android 构建、Lint、包内版本、包名、大小与 SHA-256 的原子发布校验已通过。
- 当前安装包仍是验证签名包；在生产 release 密钥与覆盖安装验证完成前，不得称为正式商用发布。

本总纲的后续更新必须同步修改 [需求追踪表](REQUIREMENTS_TRACEABILITY.md)、[路线图](ROADMAP.md)、对应版本发布说明和自动化验证证据。
