<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug remains compatible with local and explicitly confirmed HTTP test endpoints. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
